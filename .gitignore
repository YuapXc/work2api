# 交接文档（按用户要求不提交）
/docs/HANDOFF.md

# 参考仓库（clone 自上游，不提交）
/.reference/

# Go 构建产物
/work2api
/work2api.exe
/dist/
/bin/
*.exe
*.test
*.out

# 运行时数据（含凭证/密钥/使用记录，敏感）
/data/
*.db
*.db-shm
*.db-wal
.secret_key

# 账号凭据（含 access/refresh token，绝不提交）
/auths/
*.info

# OpenCode 供应商配置（含 zen/go 上游 API Key）+ 自动生成的模型/定价缓存。
# 默认落在 data/opencode/（已被 /data/ 覆盖）；下面兼容旧的项目根路径与自定义位置。
/opencode.json
/opencode/
*.models.catalog.json
*.models.dev.json

# 前端
/webui/node_modules/
/webui/dist/
/frontend/node_modules/

# IDE / OS
.idea/
.vscode/
.DS_Store
